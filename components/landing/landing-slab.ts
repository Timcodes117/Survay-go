/** Shared visual blocks for landing sections — use fixed heights so siblings match. */

export const landingSlabBaseClass =
  "flex w-full shrink-0 items-center justify-center rounded-[28px] bg-[#f2f2f2] px-6 py-12 sm:px-8 sm:py-14 dark:bg-zinc-800/75";

/** Main feature / flow illustration panels (3-up or 2-up rows). */
export const landingSlabFeatureHeightClass =
  "h-[300px] sm:h-[340px] lg:h-[380px]";

/** Pattern gallery phone pedestals (horizontal strip). */
export const landingSlabPatternHeightClass = "h-[360px] sm:h-[400px]";

/** Testimonial avatar strip. */
export const landingSlabTestimonialHeightClass = "h-[220px] sm:h-[240px]";
